# Смоук-скрипт MIXED (русская речь с английскими терминами — язык агентного клина).
# Читать естественно, термины произносить как привычно в работе. Одна строка = одна диктовка.
Задеплой на прод через docker compose и проверь, что healthcheck зелёный.
Открой pull request в репозитории wai-dictation и добавь описание изменений.
Переименуй переменную userId в customerId во всех файлах пакета DictationCore.
Посмотри логи в Sentry: там опять timeout при запросе к Postgres.
Сделай git commit с сообщением «fix: race condition в hotkey handler» и запушь в main.
Запусти swift test в обоих пакетах перед тем, как собирать DMG.
Добавь в Makefile таргет для lint и подключи его к CI на GitHub Actions.
Напиши промпт для Claude Code: отрефактори TextInserter, не трогая протокол буфера обмена.
Скачай модель Parakeet с Hugging Face и проверь SHA двести пятьдесят шесть у каждого файла.
Обнови зависимость FluidAudio до версии ноль пятнадцать пять и прогони бенчмарки.
